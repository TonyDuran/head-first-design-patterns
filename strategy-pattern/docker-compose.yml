version: '3.8'

services:
  strategy-pattern-demo:
    build: .
    ports:
      - "8000:8000"  # Backend API
      - "8080:8080"  # Frontend UI
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
